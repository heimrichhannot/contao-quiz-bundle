{% block body %}
    <div class="quiz-title">{{ title|raw }}</div>
    <div class="quiz-text">{{ text|raw }}</div>
    <div class="count">{{ itemsFoundText }}</div>
    <div class="quiz-question">
        {{ question|raw }}
    </div>
    {% for answer in answers %}
        {{ answer|raw }}
    {% endfor %}
{% endblock %}