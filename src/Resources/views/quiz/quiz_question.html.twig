{% block body %}
    <div class="count">{{ itemsFoundText }}</div>

    {{ question|raw }}

    {% for answer in answers %}
        <div class="answer">
            {{ answer|raw }}
        </div>
    {% endfor %}
{% endblock %}